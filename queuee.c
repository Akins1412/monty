#include "monty.h"
/**
 * _queue - prints the top of the stack
 * @tpr: stack head
 * @linumb: line_number
 * Return: no return
*/
void _queue(stack_t **tpr, unsigned int linumb)
{
	(void)tpr;
	(void)linumb;
	glob.lifi = 1;
}
